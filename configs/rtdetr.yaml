# RT-DETR-L — accuracy ceiling / architecture contrast
# Role: transformer vs CNN comparison for R3; drop batch to 8 if OOM on T4
model: rtdetr-l.pt
data: configs/visdrone5.yaml
imgsz: 640
batch: 8
seed: 42
project: runs
name: rtdetr
