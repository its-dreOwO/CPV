# CPV Vehicle-Perception — Consolidated HANDOFF

**Date:** 2026-06-23 · Branch: `pivot/vehicle-avoidance` · merge-base with `main`: `0a21a7b`
This single handoff supersedes the earlier phase-2 and plan-3 handoffs (deleted).

## Where the project is

The pivot (drone → forward-facing dashcam vehicle/pedestrian risk-advisory) is code-complete.
Plans 1–3 are done & committed.

> **Update 2026-06-24:** all three R3 detectors finished training and their weights are fetched to
> `models/{yolov8n,yolov8m,yolov10n}-best.pt` (all verified 3-class `{vehicle,person,two_wheeler}`).
> The GCP T4 VM was deleted after fetching yolov8m's epoch-41 `best.pt` (gains had plateaued; ~0.003
> mAP left). **BDD100K test eval + robustness slicing are DONE for all three** — see `reports/R3/`
> (`<model>_metrics.json`, `<model>_robustness_*.json`, and the summarized `reports/R3/RESULTS.md`).
> **yolov8m wins** the selection rule (mAP@0.5 0.683, FPS≥30) → ships in R4. Still pending: KITTI
> zero-shot + risk validation, both blocked on the KITTI raw download (not yet done).

### Model lineup (R3 3-model comparison)
RT-DETR-L was **swapped for YOLOv10n** on budget grounds (commit `814b5c7`; rationale in
`docs/training_pipeline.md` + design-spec amendment). Final lineup, all 50 epochs, seed 42, 640px:

> **All three are now fetched & verified (2026-06-24)** — table kept for provenance.

| Model | Role / axis | Where it trained | Status |
|-------|-------------|------------------|--------|
| **yolov8n** | speed floor / shared anchor | Modal (detached) app `ap-B42LRLSyy5kC6173bbyGDq` | ✅ `models/yolov8n-best.pt` |
| **yolov8m** | primary R4 demo / capacity | GCP T4 VM `cpv-yolov8m-t4` (`us-west4-a`) — **deleted** | ✅ `models/yolov8m-best.pt` (epoch-41 `best.pt`) |
| **yolov10n** | NMS-free end-to-end / version·paradigm | Modal (detached) app `ap-5kbyPhoOuEFXmWJaqzWDol` | ✅ `models/yolov10n-best.pt` |

Detached Modal apps + the VM `nohup` all survive the operator's laptop rebooting to Windows.
If `modal app list` shows an app `stopped` before it finished, just re-run its `modal run --detach … --fresh`.

### ETA / cost
- Modal nanos: ~6–7 h each on L4, ~$5.6 each. yolov8n was relaunched `--fresh` (its earlier
  attached run's ~12 epochs were uncommitted and lost — negligible).
- GCP yolov8m: ~33 h on the T4 (~40 min/epoch), **free** (GCP credits, $172, **expire 25/06**).
- Modal budget $28.57 → spending ~$11–13 (two nanos); GCP covers yolov8m. Plenty of buffer.

## Fetch yolov8m from the GCP VM — ✅ DONE (2026-06-24)
Fetched the epoch-41 `best.pt` (training was at 42/50, plateaued) and **deleted the VM** to stop
billing, well ahead of the 25/06 credit expiry. Kept for provenance:
```bash
gcloud config set project project-b9a4f950-85a4-48f0-9ee   # NOTE: config previously had a typo (48fo→48f0)
gcloud compute scp cpv-yolov8m-t4:~/runs/yolov8m/weights/best.pt models/yolov8m-best.pt --zone=us-west4-a
gcloud compute instances delete cpv-yolov8m-t4 --zone=us-west4-a --quiet   # ✅ done — VM gone, boot disk auto-removed
```

## After each model is fetched (operator's "one-at-a-time verify")
1. **Verify classes:** `python -c "from ultralytics import YOLO; print(YOLO('models/<m>-best.pt').names)"`
   → must be `{0:'vehicle',1:'person',2:'two_wheeler'}` (NOT stale 5-class VisDrone weights).
2. **Test:** `python scripts/evaluate.py --weights models/<m>-best.pt --data configs/bdd100k.yaml --split test --classwise --output reports/R3/<m>_metrics.json` + a quick `python main.py --source data/samples/... --weights models/<m>-best.pt` smoke test.

## Remaining Plan-3 operator work (after weights are in `models/`)
- ✅ **BDD100K test eval** (×3): done → `reports/R3/<m>_metrics.json` (+ summarized `reports/R3/RESULTS.md`).
- ✅ **Robustness** (×3): done, by timeofday + weather → `reports/R3/<m>_robustness_{timeofday,weather}.json`.
- ✅ **Model selection** for R4: yolov8m wins (mAP@0.5 0.683, FPS≥30) — ships.
- ⬜ **KITTI** (blocked on download): download raw → `data/raw/kitti/{image_2,label_2}` → `scripts/preprocess_kitti.py` →
  validate → `scripts/evaluate.py --data configs/kitti.yaml --split val …` (zero-shot) +
  `scripts/validate_risk.py --output reports/R3/risk_validation.json`.

## Remaining code/process work
- **FINAL whole-branch review** — not yet run. `scripts/review-package 0a21a7b HEAD`, dispatch
  `superpowers:requesting-code-review`'s reviewer on **opus**. Triage Minor findings in `.superpowers/sdd/progress.md`.
- **Finish the branch** — `superpowers:finishing-a-development-branch` to merge/PR `pivot/vehicle-avoidance` → `main`.

## Infra notes (for resuming, possibly from a fresh context)
- **GCP:** project `project-b9a4f950-85a4-48f0-9ee`, billing acct `0126D1-706634-78FA96`. GPU quota:
  `GPUS_ALL_REGIONS=1`, per-region `NVIDIA_T4_GPUS=1`. T4 on-demand was stocked out in ~30 zones;
  `us-west4-a` had capacity. Image: `common-cu129-ubuntu-2204-nvidia-580` (driver preinstalled);
  needed `apt install python3.10-venv libgl1-mesa-glx libglib2.0-0` + `pip install ultralytics` in `~/venv`.
  Dataset was uploaded as the dereferenced tarball and extracted to `~/data/processed/bdd100k`.
- **Modal:** `modal_train.py` now unpacks the volume tarball to **local** container disk per-run
  (`/root/data`) — Modal Volumes are too slow for YOLO's per-epoch small-file reads (commit `0fd1da6`).
  Dataset archive lives in volume `cpv-bdd100k` as `/processed.tar.gz` (build with `tar czhf` — `-h`
  dereferences the symlinked images).
- **Local scratch tarball:** `…/scratchpad/processed.tar.gz` (4.3 GB, dereferenced) — reusable for re-upload.

## Recent commits this session
`a4e4b11` scripts/__init__ + docstring cleanup · `143e617` commit Plan-2 docs ·
`0fd1da6` Modal local-disk extraction + symlink-deref fix · `814b5c7` RT-DETR→YOLOv10n swap.
