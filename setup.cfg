[flake8]
max-line-length = 88
extend-ignore = E203, W503
exclude =
    .git,
    __pycache__,
    .venv,
    build,
    dist,
    notebooks,
    CPV_notes

[tool:pytest]
testpaths = tests
addopts = --cov=src --cov-report=term-missing
